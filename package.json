{
  "name": "echappee",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node copier-fonctions.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@netlify/blobs": "^10.7.11",
    "@types/three": "^0.170.0",
    "esbuild": "^0.28.1",
    "playwright": "^1.62.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
